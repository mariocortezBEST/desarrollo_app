import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'ejemplo10',
      home: Scaffold(
        appBar: AppBar(
          title: Text('ejemplo10'),
        ),
        body: Center(
          child: Container(
            width: 180.0,
            height: 120.0,
            decoration: BoxDecoration(
              color: Colors.orange[200], // Color de fondo
              border: Border.all(
                color: Colors.deepOrange, // Color del borde
                width: 3.0,               // Ancho del borde
              ),
              borderRadius: BorderRadius.circular(15.0), // Bordes redondeados
              boxShadow: [
                BoxShadow(
                  color: Colors.grey.withOpacity(0.6), // Color de la sombra
                  spreadRadius: 3,                      // Expansión de la sombra
                  blurRadius: 7,                        // Difusión de la sombra
                  offset: Offset(0, 5),                 // Desplazamiento de la sombra
                ),
              ],
            ),
            child: Center(
              child: Text(
                'Decorado',
                style: TextStyle(fontSize: 16),
              ),
            ),
          ),
        ),
      ),
    );
  }
}