import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Row con Botón y Texto',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Ejemplo2'),
        ),
        body: Center(
          child: Row(
            mainAxisAlignment: MainAxisAlignment.spaceAround,
            children: [
              ElevatedButton(
                onPressed: () {
                  // Acción al presionar
                },
                child: Text('Botón 1'),
              ),
              Text(
                'Texto Central',
                style: TextStyle(fontSize: 16),
              ),
              Icon(
                Icons.favorite,
                color: Colors.red,
              ),
            ],
          ),
        ),
      ),
    );
  }
}