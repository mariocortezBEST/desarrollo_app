import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'ejemplo6',
      home: Scaffold(
        appBar: AppBar(
          title: Text('ejemplo6'),
        ),
        body: Center(
          child: Container(
            width: 200.0, // Ancho de 200 píxeles
            height: 150.0, // Alto de 150 píxeles
            color: Colors.orange[200], // Color para visualizar el container
            child: Center(
              child: Text(
                'Tamaño Fijo',
                style: TextStyle(fontSize: 16),
              ),
            ),
          ),
        ),
      ),
    );
  }
}